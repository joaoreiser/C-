#include <iostream>
#include <vector>
#include <string>

using std::cout;
using std::endl;
using std::cin;
using std::vector;
using std::string;

/*
	STREAMS
-> the way that C++ manages IO of data

-->INPUT_SOURCE-->INPUT_STREAM-->PROGRAM-->OUTPUT_STREAM-->OUTPUT_SINK

- INPUT_SOURCE: keyboard, file, network, program, pin
- OUTPUT_SINK: console, file, network, program, pin
- INPUT_STREAM: "buffer" of input data
- OUTPUT_STREAM: "buffer" of output data

ex:

cout << "Something";

cout -> output sink (console)
<< -> operator of atribution for the output stream

cin >> variable;

cin -> input source (keyboard)
>> -> operator of atribution for the input stream

*/

int main()
{

	return 0;
}
